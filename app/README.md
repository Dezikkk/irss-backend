[Project structure]
The purpose of each folder in the `app` directory is the following:
`database` -> Files related to SQL: queries to initialize the database and a tiny little bit of "documentation"
`routes` -> Routes

